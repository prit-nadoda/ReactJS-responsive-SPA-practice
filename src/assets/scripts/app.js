
const resNav = () => {
    
    const resIcon = document.querySelector(".res-icon");
const navExp = document.querySelector(".menu-expend");

    resIcon.classList.toggle("res-icon-active");
    navExp.classList.toggle("menu-expend-active");

    
}

export default resNav;
